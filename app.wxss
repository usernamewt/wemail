/**app.wxss**/
.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  box-sizing: border-box;
} 

:root {
  --tab-bar-height: 300rpx;
}
.page {
  padding-bottom: calc(166rpx + env(safe-area-inset-bottom));
}
